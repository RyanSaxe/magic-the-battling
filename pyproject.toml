[project]
name = "magic-the-battling"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.6",
    "requests>=2.32.3",
]

[dependency-groups]
dev = [
    "magic-the-battling",
    "pytest>=8.3.4",
    "ruff>=0.8.6",
]

[tool.uv.sources]
magic-the-battling = { workspace = true }

[tool.ruff]
line-length = 120