name: Release

on:
  workflow_dispatch:
    inputs:
      bump_type:
        description: 'Version bump type'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --group dev

      - name: Run ruff linting
        run: uvx ruff check .

      - name: Run ruff format check
        run: uvx ruff format --check .

      - name: Run ty type check
        run: uvx ty check

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --group dev

      - name: Run tests
        run: uv run pytest

  lint-frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Run TypeScript check
        run: npm run typecheck

      - name: Build
        run: npm run build

  release:
    needs: [lint, test, lint-frontend]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      APP_BASE_URL: https://magic-the-battling.fly.dev
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get latest tag
        id: get_tag
        run: |
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          echo "latest_tag=$LATEST_TAG" >> $GITHUB_OUTPUT

      - name: Calculate new version
        id: calc_version
        run: |
          LATEST_TAG="${{ steps.get_tag.outputs.latest_tag }}"
          VERSION=${LATEST_TAG#v}
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"

          case "${{ github.event.inputs.bump_type }}" in
            major)
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
              ;;
            minor)
              MINOR=$((MINOR + 1))
              PATCH=0
              ;;
            patch)
              PATCH=$((PATCH + 1))
              ;;
          esac

          NEW_VERSION="v${MAJOR}.${MINOR}.${PATCH}"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create ${{ steps.calc_version.outputs.new_version }} \
            --title "${{ steps.calc_version.outputs.new_version }}" \
            --generate-notes

      - name: Deploy to fly.io
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Enter draining mode
        run: |
          curl --fail --show-error --silent \
            -X POST "${APP_BASE_URL}/api/ops/mode" \
            -H "Content-Type: application/json" \
            -H "x-ops-token: ${OPS_API_TOKEN}" \
            -d '{"mode":"draining","message":"Server updating. Please wait a moment while we reconnect your game.","updated_by":"github-actions-release"}'
        env:
          OPS_API_TOKEN: ${{ secrets.OPS_API_TOKEN }}

      - name: Wait for client notice window
        run: sleep 60

      - name: Run fly deploy
        run: flyctl deploy --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Return to normal mode
        if: success()
        run: |
          curl --fail --show-error --silent \
            -X POST "${APP_BASE_URL}/api/ops/mode" \
            -H "Content-Type: application/json" \
            -H "x-ops-token: ${OPS_API_TOKEN}" \
            -d '{"mode":"normal","message":"", "updated_by":"github-actions-release"}'
        env:
          OPS_API_TOKEN: ${{ secrets.OPS_API_TOKEN }}

      - name: Switch to maintenance mode on deploy failure
        if: failure()
        run: |
          curl --fail --show-error --silent \
            -X POST "${APP_BASE_URL}/api/ops/mode" \
            -H "Content-Type: application/json" \
            -H "x-ops-token: ${OPS_API_TOKEN}" \
            -d '{"mode":"maintenance","message":"Deployment did not complete. Please retry in a few minutes.","updated_by":"github-actions-release"}'
        env:
          OPS_API_TOKEN: ${{ secrets.OPS_API_TOKEN }}
